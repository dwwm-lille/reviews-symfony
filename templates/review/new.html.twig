{% extends 'base.html.twig' %}

{% block body %}
<div class="my-8">
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="bg-slate-500 text-white py-2 px-3 hover:bg-slate-400 rounded-lg block w-full mt-10">Envoyer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
