{% use 'tailwind_2_layout.html.twig' %}

{% block widget_attributes %}
    {% set widget_class = widget_class|default('mt-1 w-full rounded-lg') %}
    {{ parent() }}
{% endblock %}

{% block form_errors %}
    {% set error_item_class = error_item_class|default('text-red-700 text-xs') %}
    {{ parent() }}
{% endblock %}
